# Copia a ../../.env (server/supabase/.env) para `supabase functions serve`.
# En producción NO se usa este archivo: los valores van como secrets
# (`supabase secrets set`, ver ../../../README.md).
GROQ_API_KEY=gsk_...
GEMINI_API_KEY=AQ....
